import React, { Fragment } from 'react';

const MovieCard = props => {
  // console.log(props);
  
  return (
    <Fragment>
      <h3>... {this.props}</h3>
      
    </Fragment>
  )
};

export default MovieCard;